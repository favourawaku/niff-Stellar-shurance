{
  "__inputs": [
    {
      "name": "DS_PROMETHEUS",
      "label": "Prometheus",
      "description": "",
      "type": "datasource",
      "pluginId": "prometheus",
      "pluginName": "Prometheus"
    }
  ],
  "title": "NiffyInsure API",
  "uid": "niffyinsure-api",
  "schemaVersion": 38,
  "version": 1,
  "refresh": "30s",
  "panels": [
    {
      "id": 1,
      "title": "Request Rate (req/s)",
      "type": "timeseries",
      "gridPos": { "x": 0, "y": 0, "w": 12, "h": 8 },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "sum(rate(http_requests_total{app=\"niffyinsure-api\"}[1m])) by (route, method)",
          "legendFormat": "{{method}} {{route}}"
        }
      ]
    },
    {
      "id": 2,
      "title": "HTTP Latency p50 / p95 / p99 (s)",
      "type": "timeseries",
      "gridPos": { "x": 12, "y": 0, "w": 12, "h": 8 },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.50, sum(rate(http_request_duration_seconds_bucket{app=\"niffyinsure-api\"}[5m])) by (le, route))",
          "legendFormat": "p50 {{route}}"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{app=\"niffyinsure-api\"}[5m])) by (le, route))",
          "legendFormat": "p95 {{route}}"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket{app=\"niffyinsure-api\"}[5m])) by (le, route))",
          "legendFormat": "p99 {{route}}"
        }
      ]
    },
    {
      "id": 3,
      "title": "5xx Error Rate",
      "type": "timeseries",
      "gridPos": { "x": 0, "y": 8, "w": 12, "h": 8 },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "sum(rate(http_5xx_errors_total{app=\"niffyinsure-api\"}[5m])) by (route)",
          "legendFormat": "5xx {{route}}"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "red", "value": 0.1 }
            ]
          }
        }
      }
    },
    {
      "id": 4,
      "title": "RPC Call Rate by Method",
      "type": "timeseries",
      "gridPos": { "x": 12, "y": 8, "w": 12, "h": 8 },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "sum(rate(rpc_calls_total{app=\"niffyinsure-api\"}[1m])) by (rpc_method, status)",
          "legendFormat": "{{rpc_method}} ({{status}})"
        }
      ]
    },
    {
      "id": 5,
      "title": "RPC Error Rate",
      "type": "timeseries",
      "gridPos": { "x": 0, "y": 16, "w": 12, "h": 8 },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "sum(rate(rpc_errors_total{app=\"niffyinsure-api\"}[5m])) by (rpc_method, error_type)",
          "legendFormat": "{{rpc_method}} / {{error_type}}"
        }
      ]
    },
    {
      "id": 6,
      "title": "RPC Latency p95 (s)",
      "type": "timeseries",
      "gridPos": { "x": 12, "y": 16, "w": 12, "h": 8 },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.95, sum(rate(rpc_call_duration_seconds_bucket{app=\"niffyinsure-api\"}[5m])) by (le, rpc_method))",
          "legendFormat": "p95 {{rpc_method}}"
        }
      ]
    },
    {
      "id": 7,
      "title": "Node.js Heap Used (MB)",
      "type": "timeseries",
      "gridPos": { "x": 0, "y": 24, "w": 12, "h": 8 },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "nodejs_heap_size_used_bytes{app=\"niffyinsure-api\"} / 1024 / 1024",
          "legendFormat": "heap used MB"
        }
      ]
    },
    {
      "id": 8,
      "title": "Event Loop Lag (s)",
      "type": "timeseries",
      "gridPos": { "x": 12, "y": 24, "w": 12, "h": 8 },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "nodejs_eventloop_lag_seconds{app=\"niffyinsure-api\"}",
          "legendFormat": "event loop lag"
        }
      ]
    },
    {
      "id": 9,
      "title": "p95 Latency by Route & Status (SLA view)",
      "description": "p95 latency per route broken down by HTTP status class. Use the 1 s threshold line to monitor SLA compliance.",
      "type": "timeseries",
      "gridPos": { "x": 0, "y": 32, "w": 24, "h": 8 },
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "yellow", "value": 0.5 },
              { "color": "red", "value": 1.0 }
            ]
          },
          "custom": { "thresholdsStyle": { "mode": "line" } }
        }
      },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{app=\"niffyinsure-api\"}[5m])) by (le, route, status_code))",
          "legendFormat": "p95 {{route}} HTTP{{status_code}}"
        }
      ]
    },
    {
      "id": 10,
      "title": "SLA Compliance — requests ≤ 1 s (%)",
      "description": "Percentage of HTTP requests completed within 1 second, grouped by route. Drop below 99 % triggers the HighP95Latency alert.",
      "type": "timeseries",
      "gridPos": { "x": 0, "y": 40, "w": 24, "h": 8 },
      "fieldConfig": {
        "defaults": {
          "unit": "percentunit",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "red",    "value": null },
              { "color": "yellow", "value": 0.95 },
              { "color": "green",  "value": 0.99 }
            ]
          },
          "custom": { "thresholdsStyle": { "mode": "line" } }
        }
      },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "expr": "sum(rate(http_request_duration_seconds_bucket{app=\"niffyinsure-api\",le=\"1\"}[5m])) by (route) / sum(rate(http_request_duration_seconds_count{app=\"niffyinsure-api\"}[5m])) by (route)",
          "legendFormat": "SLA ≤1s {{route}}"
        }
      ]
    }
  ]
}
